import React from 'react';

class SearchButton extends React.Component {
  render(): React.ReactNode {
    return <button className="search-form__button"></button>;
  }
}

export default SearchButton;
